# ==============================================================================
# Gameplay Analysis Toolkit - Environment Configuration Template
# ==============================================================================
# Copy this file to .env and customize the values for your project
# ==============================================================================

# ------------------------------------------------------------------------------
# OpenAI API Configuration
# ------------------------------------------------------------------------------
# Required for GPT-4V HUD extraction, GPT-4o transcription, and outcome detection
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# ------------------------------------------------------------------------------
# CLIP Action Labels Configuration
# ------------------------------------------------------------------------------
# Custom labels for CLIP zero-shot action classification (pipe-delimited)
# These labels are used by detect-games to identify game boundaries and actions
#
# Default labels (18 total):
# - 13 gameplay actions (combat, looting, movement, etc.)
# - 5 game state transitions (menu, victory, defeat, loading)
#
# IMPORTANT: Customize these labels based on your specific game
# The following are optimized for battle royale games
CLIP_LABELS=player looting items and equipment indoors|player in active combat shooting at enemies|player driving a vehicle on road|player running and rotating between zones|player healing and managing inventory|player hiding and taking cover in building|helicopters visible on screen before parachute drop|player parachuting and landing|player aiming down sights at enemy|player in open field moving tactically|player dead or spectating teammates|player using inventory menu|player inside vehicle as passenger|main menu or lobby screen|victory screen showing final placement|defeat screen or player eliminated|post-game statistics and summary screen|loading screen or match starting

# ------------------------------------------------------------------------------
# CLAP Audio Event Labels Configuration
# ------------------------------------------------------------------------------
# Custom labels for CLAP zero-shot audio event classification (pipe-delimited)
# These labels are used by detect-audio-events to classify gameplay audio
#
# Default labels (30+ total):
# - Weapon sounds (gunshots, rifle fire, shotgun, etc.)
# - Movement sounds (footsteps, running, jumping, landing)
# - Combat sounds (explosions, melee, shield break, etc.)
# - Environment sounds (doors, vehicles, ambient, etc.)
# - Communication (voice chat, pings, callouts)
#
# IMPORTANT: Customize these labels based on the audio events in your game
# The following are optimized for FPS/Battle Royale games
CLAP_LABELS=gunshots and weapon fire|automatic rifle firing|shotgun blast sound|sniper rifle single shot|pistol rapid fire|weapon reload sound|grenade throw and pin pull|explosions and blast sounds|rocket launcher or explosive weapon|footsteps on various surfaces|player running and sprinting|player jumping and landing impact|player crouching and sneaking|vehicle engine and driving sounds|helicopter or aircraft sounds|door opening and closing|armor or shield breaking|player taking damage or hurt sounds|melee weapon swings and impacts|healing or medical item usage|looting and item pickup sounds|voice chat and team communication|ping markers and callout sounds|ambient game environment|wind and outdoor atmosphere|indoor room ambience|victory or defeat music|menu navigation and UI sounds|parachute deployment sound|zone or storm damage sounds

# ------------------------------------------------------------------------------
# Audio Transcription Configuration (GPT-4o)
# ------------------------------------------------------------------------------
# Optional: Override default GPT-4o transcription model
# GPT4O_TRANSCRIBE_MODEL=gpt-4o-audio-preview

# Optional: Chunk duration for parallel audio processing (seconds)
# GPT4O_CHUNK_DURATION=300.0

# Optional: Maximum parallel workers for audio transcription
# GPT4O_MAX_WORKERS=4

# Optional: Temperature for transcription (0.0 = deterministic)
# GPT4O_TEMPERATURE=0.0

# Optional: Audio voice for output
# GPT4O_AUDIO_VOICE=alloy

# Optional: Custom transcription prompt
# GPT4O_TRANSCRIPTION_PROMPT=Please transcribe this audio file accurately.

# ------------------------------------------------------------------------------
# Optional: Advanced Model Configuration
# ------------------------------------------------------------------------------
# CLIP model architecture (default: ViT-B/32)
# Options: ViT-B/32 (fast), ViT-B/16 (balanced), ViT-L/14 (accurate, slow)
# CLIP_MODEL=ViT-B/32

# GPT-4V vision model (default: gpt-4o)
# GPT4V_MODEL=gpt-4o

# ------------------------------------------------------------------------------
# Optional: Game Detection Thresholds
# ------------------------------------------------------------------------------
# Frame sampling interval for CLIP classification (seconds, default: 2.0)
# CLIP_INTERVAL=2.0

# Minimum game duration for detection (seconds, default: 60.0)
# MIN_GAME_DURATION=60.0

# Parachute confidence threshold for game start (default: 0.85)
# Higher = fewer false positives, may miss some starts
# PARACHUTE_THRESHOLD=0.85

# Death confidence threshold for game end (default: 0.60)
# Lower than parachute because death screens can be brief
# DEATH_THRESHOLD=0.60
