# GPU-Accelerated Requirements
# Install with: pip install -r requirements-gpu.txt
# Requires NVIDIA GPU with CUDA support

# Include base requirements (except CPU-only packages)
-r requirements.txt

# ============================================================================
# GPU-Accelerated Packages
# ============================================================================

# PaddlePaddle with GPU support (CUDA 11.x)
# NOTE: Not available for Python 3.13+ yet. Use Python 3.11 or 3.12, or use CPU version
# paddlepaddle-gpu>=2.5.0

# PyTorch with GPU support (CUDA 11.8)
# Note: Adjust based on your CUDA version
# See: https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0

# ============================================================================
# Installation Notes
# ============================================================================

# 1. Check your CUDA version:
#    nvidia-smi
#
# 2. Install matching PyTorch version:
#    CUDA 11.8: Use cu118
#    CUDA 12.1: Use cu121
#
# 3. For PaddlePaddle GPU:
#    pip install paddlepaddle-gpu -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 4. Verify installation:
#    python -c "import paddle; print(paddle.device.cuda.device_count())"
#    python -c "import torch; print(torch.cuda.is_available())"
