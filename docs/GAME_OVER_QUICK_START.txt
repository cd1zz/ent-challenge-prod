╔══════════════════════════════════════════════════════════════════════════════╗
║              GAME OVER DETECTION WITH PADDLEOCR - QUICK START                ║
╚══════════════════════════════════════════════════════════════════════════════╝

Automatically detect game over screens and extract stats using PaddleOCR.
No manual configuration needed - just point it at your video!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 WHAT IT DOES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Scans video for game over/end screens
2. Detects outcome (victory, defeat, eliminated)
3. Extracts visible stats:
   - Placement/rank (#1, #5, etc.)
   - Kills
   - Damage
   - Survival time
   - Assists, revives, etc.

⚡ INSTALLATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

pip install paddlepaddle paddleocr opencv-python


🚀 BASIC USAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Scan entire video
python detect_game_over_paddleocr.py video.mkv -o results.json

# With GPU (10x faster)
python detect_game_over_paddleocr.py video.mkv --gpu -o results.json

# Verbose output (see what's detected)
python detect_game_over_paddleocr.py video.mkv -v -o results.json


📊 EXAMPLE OUTPUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Detected events:

1. Timestamp: 567.5s
   Outcome: eliminated (confidence: high)
   Placement: #5
   Kills: 3
   Damage: 487
   Survival Time: 15:30

2. Timestamp: 1234.2s
   Outcome: victory (confidence: high)
   Placement: #1
   Kills: 7
   Damage: 892
   Survival Time: 23:45


🎮 HOW IT WORKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Screen Detection - Looks for keywords:
   • Victory: winner, victory, chicken dinner, #1, champion
   • Defeat: eliminated, died, defeated, game over
   • Stats: kills, damage, time, placement

2. Outcome Classification:
   • Victory - Win screen detected
   • Eliminated - Death screen detected
   • Ended - Generic end (unclear outcome)

3. Stats Extraction - Pattern matching:
   • Placement: "#5", "5th place", "Rank: 5"
   • Kills: "Kills: 3", "3 Kills", "K: 3"
   • Damage: "Damage: 487", "487 DMG"
   • Time: "15:30", "15m 30s"


💡 COMMON USE CASES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

USE CASE 1: Multiple Games in One Video
  # Automatically find all game endings
  python detect_game_over_paddleocr.py long_session.mkv --gpu -o session.json

USE CASE 2: Performance Tracking
  # Extract stats from all games for analysis
  python detect_game_over_paddleocr.py gameplay.mkv -o stats.json
  # Parse JSON to calculate averages

USE CASE 3: Highlight Reel Creation
  # Find all victories
  python detect_game_over_paddleocr.py stream.mkv -o results.json
  # Filter for outcome == "victory"

USE CASE 4: Auto-Clipping
  # Get timestamps, then use ffmpeg to extract clips
  ffmpeg -ss 500 -to 567.5 -i video.mkv -c copy game_1.mkv


🔧 ADVANCED OPTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Scan only last 5 minutes
python detect_game_over_paddleocr.py video.mkv --start-time 1500 -o results.json

# Sample every 5 seconds (faster, might miss short screens)
python detect_game_over_paddleocr.py video.mkv --interval 5.0 -o results.json

# Save screenshots of detected screens
python detect_game_over_paddleocr.py video.mkv --save-frames -o results.json

# Multi-language support
python detect_game_over_paddleocr.py video.mkv --lang ch -o results.json

# Adjust sensitivity
python detect_game_over_paddleocr.py video.mkv --confidence 0.4 -o results.json


📈 PERFORMANCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

10-minute video (300 frames @ 2s intervals):
  CPU (Intel i7):  ~5 minutes
  GPU (RTX 3080):  ~45 seconds

Memory usage:
  CPU: ~2GB
  GPU: ~3GB


✅ ADVANTAGES OVER OTHER METHODS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

vs GPT-4V:
  ✓ FREE (no API costs)
  ✓ Faster processing
  ✓ Works offline
  ✓ No API key needed

vs CLIP:
  ✓ Extracts actual stats (not just detection)
  ✓ More accurate text reading
  ✓ Gets specific numbers

vs Manual:
  ✓ Automated
  ✓ Processes hours of video in minutes
  ✓ No human error


🛠️ TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Problem: No game over screens detected
Fix: python detect_game_over_paddleocr.py video.mkv --confidence 0.4 -v -o results.json

Problem: False positives (detecting game over when it's not)
Fix: python detect_game_over_paddleocr.py video.mkv --confidence 0.8 -o results.json

Problem: Stats not extracted
Fix: Check verbose output with -v to see detected text


📖 FULL DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

See GAME_OVER_DETECTION_GUIDE.md for:
  • Detailed usage examples
  • Output format explanation
  • Integration with other tools
  • Performance optimization
  • Custom keyword matching
  • Tournament analysis workflows


🎯 QUICK WORKFLOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Install PaddleOCR
   pip install paddlepaddle paddleocr

2. Scan your video
   python detect_game_over_paddleocr.py video.mkv --gpu -v -o results.json

3. Check output
   cat results.json

4. Use the data
   # Parse JSON, calculate stats, create clips, etc.

That's it! No configuration, no API keys, just results.

╔══════════════════════════════════════════════════════════════════════════════╗
║  Full guide: GAME_OVER_DETECTION_GUIDE.md                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝
